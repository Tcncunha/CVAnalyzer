# API keys are entered in the sidebar and stored in session only.
# Optionally set defaults below so they auto-load on startup.
# Tokens are NEVER written to disk by the app.

OPENCODE_ZEN_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
